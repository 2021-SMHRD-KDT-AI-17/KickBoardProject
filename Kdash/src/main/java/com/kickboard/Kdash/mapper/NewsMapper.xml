<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kickboard.Kdash.mapper.NewsMapper">
	<select id="newsFirstList" resultType="com.kickboard.Kdash.entity.News">
		select * from tb_article order by article_idx DESC limit 25
	</select>
	<select id="newsPreList" parameterType="int" resultType="com.kickboard.Kdash.entity.News">
		select * from tb_article where #{num}>article_idx order by article_idx DESC limit 25
	</select>
	<select id="newsPostList" parameterType="int" resultType="com.kickboard.Kdash.entity.News">
		select * from tb_article where #{num}+25>=article_idx order by article_idx DESC limit 25
	</select>
</mapper>